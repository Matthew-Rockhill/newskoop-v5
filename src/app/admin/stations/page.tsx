import { StationsContent } from '@/components/admin/StationsContent';

export const metadata = {
  title: 'Radio Stations - Newskoop Admin',
};

export default function StationsPage() {
  return <StationsContent />;
} 